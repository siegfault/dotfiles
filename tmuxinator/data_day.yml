# ~/.tmuxinator/data_day.yml

name: data_day
root: ~/code/data_day

windows:
  - rails:
      layout: main-vertical
      panes:
        - bundle exec puma -b tcp://127.0.0.1:3002
        - bundle exec rails c
        - bundle exec rails db
  - vim:
      panes:
        - vim
  - terminal:
