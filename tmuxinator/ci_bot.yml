# ~/.tmuxinator/ci_bot.yml

name: ci_bot
root: ~/code/ci_bot

windows:
  - rails:
      layout: tiled
      panes:
        - bundle exec puma -b tcp://127.0.0.1:3003
        - bundle exec rails c
        - tail -f log/development.log
        - bundle exec rails db
  - vim:
      panes:
        - vim
  - terminal:
